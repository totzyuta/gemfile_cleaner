require 'spec_helper'

describe GemfileCleaner do
  it 'has a version number' do
    expect(GemfileCleaner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
